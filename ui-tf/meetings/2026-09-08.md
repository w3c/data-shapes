# Meeting on 8 September 2026

## Attendees

- Daniel Beeke
- Thomas Bergwinkl
- Robert David
- Edmond Chuc

## Agenda

### Topic: Pull requests updates

Any pull requests that people who like the task force's attention on?

### Topic: Conformance discussion from yesterday's plenary meeting
* Carine advised using fallbacks rather than failures for parts of the specification that an implementation does not support.
* Carine advised that an implementation not passing all tests does not necessarily mean that it is non-conformant with SHACL UI.
* Carine advised that implementations are expected to implement the normative parts of the specification.
* Carine advised that implementations may cherry-pick features, particularly for a user interface specification. She contrasted this with specifications such as WebRTC, where implementations are generally expected to follow the specification more strictly. The appropriate approach depends on the nature of the specification, and she suggested that user interface features should be driven by use cases.
* Carine advised that the WG should define features and their dependencies as groups, so that an implementation choosing to support feature B understands that it must also support any prerequisite feature A.
* Carine advised that, for a user interface specification, it may make sense to define conformance around the use cases that are in scope.
* The co-chairs suggested that, from this week onward, no new use cases should be added unless all members of the task force agree to support them.

### Topic: Scoring shape of shui:TextFieldEditor

While implementing I found that some property shape would not receive any widgets. See [1231](https://github.com/w3c/data-shapes/issues/1231)


### Topic: Interface labels

I think we should split up interface languages from content languages. See [1208](https://github.com/w3c/data-shapes/issues/1208)

### Topic: Distinguish between UI and non-UI shapes

Should we add something like `shui:purpose` or `sh:purpose`? See [332](https://github.com/w3c/data-shapes/issues/332)

## Minutes

Done with those two wording changes:

## Minutes

### Topic: Pull request updates

**Robert:** On the read-only graph, I’d like to clarify the question with you, Edmond.

**Edmond:** It should be a conceptual input to the SHACL Renderer and should not be defined in the global configuration.

**Robert:** How come the label resolution properties are not required as input parameters to the SHACL Renderer?

**Edmond:** Label resolution preferences are defined directly on property shapes using property roles.

**Bergi:** The label resolution global configuration needs to be provided as parameters to the label resolution process.

**Daniel:** What is the purpose of the global configuration? Some users may ship global configuration with their data, so should the application be expected to honour it? Is global configuration something defined by the user or by the application?

**Bergi:** Instead of having a data graph, we could have a data dataset.

**Edmond:** I would advise against using datasets. SHACL Core does not support datasets in this way, and having conceptual inputs for the data graph, shapes graph, and read-only graph is more in line with SHACL Core.

**Robert:** Putting it in the global configuration may allow it to be used for rules or elsewhere outside of the UI. My use case is to override the behaviour using global configuration for UI purposes, regardless of whether the SHACL shapes define the relevant label roles.

**Daniel:** I would define this on the property shape using a property role.

**Bergi:** The shapes graph is often effectively a union graph composed from different sources. For parameters, we could define an optional set of parameters to the SHACL Renderer.

**Daniel:** We should follow the W3C approach to specifying features: define what the feature does and keep the mechanism open-ended where possible.

**Robert:** Should we still have a global configuration now that we have converged on this approach?

### Topic: Conformance discussion from yesterday’s plenary meeting

**Bergi:** The first step is to agree on the use cases.

**Action:** Establish a table of use cases, including which use cases are in scope and which are out of scope.

**Daniel:** We should establish a table of use cases and clearly identify what is in scope and what is not. However, I would prefer a different approach to conformance.

**Action:** Establish a date by which the Task Force will decide on the agreed set of use cases.
