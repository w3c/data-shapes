# Meeting on 18 August 2026

## Agenda

- SHACL UI Profiling
- Is PR [#1137](https://github.com/w3c/data-shapes/pull/1137) (Accept rdf:dirLangString where rdf:langString is defined) good to be merged?
- Is PR [#1118](https://github.com/w3c/data-shapes/pull/1118) (Split focus node and node shape resolution from rendering) good to be merged?
- Global configuration (PR [#900](https://github.com/w3c/data-shapes/pull/900))
  1. Discussion on `shui:shapesLabelPreference` and `shui:dataLabelPreference` vs. single `shui:labelPreference`.
  2. Should we make it clear what timezone format should/must be used? (Raised by Edmond)
  3. How could one have shapes with a certain `shui:defaultNamespace` and another shape with a different `shui:defaultNamespace`? (Raised by Daniel)
     - Proposal by Ieben: create issue for that and follow-up on that after this PR.
  4. Discussion on having a `shui:inferenceGraph` or `shui:readOnlyGraph` (Leftover in issue #869 but not yet addressed in the PR).
     - Do we need this at all?
     - Proposal by Ieben: create new issue for that and follow-up on that after this PR. Then we can discuss its relevance in that issue first (ideally with linked use cases).
- PR [#1143](https://github.com/w3c/data-shapes/pull/1143) (Extend shui:hasDatatypeNumericConstraint with more numeric datatypes)
- PR [#1151](https://github.com/w3c/data-shapes/pull/1151) (Add Registries section referencing the community widget registry)
  - Discussion in issue [#718](https://github.com/w3c/data-shapes/issues/718)
  - Open question: Community namespace: `http://www.w3.org/ns/shacl-ui-community/` or `https://w3id.org/shacl-ui-community/`. Others? Who to contact to set this up?
- Any input on issue [#1152](https://github.com/w3c/data-shapes/issues/1152) (Declare widget instances (Single/Multi Editor/Viewer) with labels, comments, and class hierarchy) before I create a PR for that?
- Any input on issue [#1153](https://github.com/w3c/data-shapes/issues/1153) (Create a SHACL 1.2 UI vocabulary file under shacl12-vocabularies/` before I create a PR for that?
- Any input on issue [#1141](https://github.com/w3c/data-shapes/issues/1141) (Widget scoring hasDatatype…Constraint shapes do not detect list-valued sh:datatype) before I create a PR for that?

## Attendees

## Minutes
