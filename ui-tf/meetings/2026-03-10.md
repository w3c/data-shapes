# Meeting on 10 March 2026

Attendees
-	Ieben Smessaert
-	Thomas Bergwinkl
-	Daniel Beeke
-	Edmond Chuc

## Ieben paper and co-authors

- Ieben is planning to write a paper and would like to include us as co-authors.

## Widgets Scoring PR #804

- The discussion focused on restructuring the PR so that the algorithm is defined first and the functions are introduced afterwards, rather than starting with the functions and deriving the algorithm from them.
- **Action:** Daniel will try the approach suggested by Ieben, with the functions referenced in the algorithm hyperlinked.

## Core Constraints document structure

- Add an appendix table covering core constraints and other SHACL Core concepts.
- Cross-link the appendix table to relevant UI features.
- **Action:** Edmond to create an initial PR for the appendix table.

## Topic: `sh:group` and `sh:order`

- Daniel asked whether `sh:group` should be moved into the UI namespace.
- Bergi noted that it could be deprecated, but there are trade-offs. Some implementations may already use it for use cases beyond UI.
- Ieben noted that SHACL Core already states that it is used for form-building.
- **Action:** Daniel to create an issue describing the possible move from `sh:group` and `sh:order` to `shui:group` and `shui:order`, including possible deprecation of the existing terms.

## SHACL UI error handling and mapping validation reports

- Discuss how validation results can be mapped back to the form, that is, to the data graph.
- This may be better handled as guidance rather than a normative requirement.
- Consider both client-side and server-side validation.
- Not all shapes are suitable for client-side validation, for example SPARQL-based constraints.
- Skolemisation was noted as a related consideration.
- Consider when validation should occur, such as on submission or when a field loses focus.
- Consider whether shapes should be categorised according to whether they are suitable for client-side or server-side validation.
- Not all validation errors may map cleanly to individual fields; some may need to be associated with the focus node instead.
- **Action:** Edmond to create an issue for this topic.

## SHACL Profiling

- Discuss providing guidance on how implementations may express which aspects of SHACL UI they support.
- **Action:** Edmond to create a use case issue for the Profiling subgroup.
- **Action:** Bergi to contribute to the issue.
