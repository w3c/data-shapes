# Meeting on 20 August 2026

## Attendees

## Agenda

### SHACL UI Profiling

* Is there a way to define a profile using shapes?
* If so, how does this differ from defining profiles and specifying SHACL elements using `rdfs:member`?
* When specifying dependencies between groups using `owl:imports`, how does this differ from subprofiling?
* Is SHACL UI considered a subprofile of SHACL?

**This is currently being addressed by:**
* Issue: https://github.com/w3c/data-shapes/issues/1164
* PR: https://github.com/w3c/data-shapes/pull/1173

### PR merge status

* Is PR [#1137](https://github.com/w3c/data-shapes/pull/1137) (Accept `rdf:dirLangString` where `rdf:langString` is defined) ready to be merged?
* Is PR [#1118](https://github.com/w3c/data-shapes/pull/1118) (Split focus node and node shape resolution from rendering) ready to be merged?

  * Discuss the alternative mentioned in [issue #609](https://github.com/w3c/data-shapes/issues/609#issuecomment-5319520357).

### PR discussion

* Global configuration (PR [#900](https://github.com/w3c/data-shapes/pull/900))

  1. Discuss `shui:shapesLabelPreference` and `shui:dataLabelPreference` versus a single `shui:labelPreference`.
  2. Should we make it clear which timezone format should or must be used? (Raised by Edmond)
  3. How could one have shapes with a certain `shui:defaultNamespace` and other shapes with a different `shui:defaultNamespace`? (Raised by Daniel)

     * Proposal by Ieben: create an issue for this and follow up after this PR.
  4. Discuss having a `shui:inferenceGraph` or `shui:readOnlyGraph` (left over from issue #869 but not yet addressed in the PR).

     * Do we need this at all?
     * Proposal by Ieben: create a new issue and follow up after this PR. We can first discuss its relevance there, ideally with linked use cases.
* PR [#1143](https://github.com/w3c/data-shapes/pull/1143) (Extend `shui:hasDatatypeNumericConstraint` with more numeric datatypes).
* PR [#1151](https://github.com/w3c/data-shapes/pull/1151) (Add Registries section referencing the community widget registry).

  * Discussion in issue [#718](https://github.com/w3c/data-shapes/issues/718).
  * Open question: which community namespace should we use: `http://www.w3.org/ns/shacl-ui-community/`, `https://w3id.org/shacl-ui-community/`, or something else? Who should we contact to set this up?

### Issue discussion

* Any input on issue [#1152](https://github.com/w3c/data-shapes/issues/1152) (Declare widget instances (Single/Multi Editor/Viewer) with labels, comments, and class hierarchy) before I create a PR?
* Any input on issue [#1153](https://github.com/w3c/data-shapes/issues/1153) (Create a SHACL 1.2 UI vocabulary file under `shacl12-vocabularies/`) before I create a PR?
* Any input on issue [#1141](https://github.com/w3c/data-shapes/issues/1141) (Widget scoring `hasDatatype…Constraint` shapes do not detect list-valued `sh:datatype`) before I create a PR?

## Minutes
