# Meeting on 20 August 2026

## Attendees

- Daniel Beeke
- Ieben Smessaert
- Thomas Bergwinkl
- Robert David
- Edmond Chuc

## Agenda

* Write issues/comments less wordy. Add "keep it brief" to the prompt/skill.

### SHACL UI Profiling

* Is there a way to define a profile using shapes?
* If so, how does this differ from defining profiles and specifying SHACL elements using `rdfs:member`?
* When specifying dependencies between groups using `owl:imports`, how does this differ from subprofiling?
* Is SHACL UI considered a subprofile of SHACL?

**This is currently being addressed by:**
* Issue: https://github.com/w3c/data-shapes/issues/1164
* PR: https://github.com/w3c/data-shapes/pull/1173

### PR merge status

* Is PR [#1137](https://github.com/w3c/data-shapes/pull/1137) (Accept `rdf:dirLangString` where `rdf:langString` is defined) ready to be merged?
* Is PR [#1118](https://github.com/w3c/data-shapes/pull/1118) (Split focus node and node shape resolution from rendering) ready to be merged?

  * Discuss the alternative mentioned in [issue #609](https://github.com/w3c/data-shapes/issues/609#issuecomment-5319520357) and decision in [issue #1177](https://github.com/w3c/data-shapes/issues/1177).

### PR discussion

* Global configuration (PR [#900](https://github.com/w3c/data-shapes/pull/900))

  1. Discuss `shui:shapesLabelPreference` and `shui:dataLabelPreference` versus a single `shui:labelPreference`.
  2. Should we make it clear which timezone format should or must be used? (Raised by Edmond)
  3. How could one have shapes with a certain `shui:defaultNamespace` and other shapes with a different `shui:defaultNamespace`? (Raised by Daniel)

     * Proposal by Ieben: create an issue for this and follow up after this PR.
  4. Discuss having a `shui:inferenceGraph` or `shui:readOnlyGraph` (left over from issue #869 but not yet addressed in the PR).

     * Do we need this at all?
     * Proposal by Ieben: create a new issue and follow up after this PR. We can first discuss its relevance there, ideally with linked use cases.
* PR [#1143](https://github.com/w3c/data-shapes/pull/1143) (Extend `shui:hasDatatypeNumericConstraint` with more numeric datatypes).
* PR [#1151](https://github.com/w3c/data-shapes/pull/1151) (Add Registries section referencing the community widget registry).

  * Discussion in issue [#718](https://github.com/w3c/data-shapes/issues/718).
  * Open question: which community namespace should we use: `http://www.w3.org/ns/shacl-ui-community/`, `https://w3id.org/shacl-ui-community/`, or something else? Who should we contact to set this up?

### Issue discussion

* Any input on issue [#1152](https://github.com/w3c/data-shapes/issues/1152) (Declare widget instances (Single/Multi Editor/Viewer) with labels, comments, and class hierarchy) before I create a PR?
* Any input on issue [#1153](https://github.com/w3c/data-shapes/issues/1153) (Create a SHACL 1.2 UI vocabulary file under `shacl12-vocabularies/`) before I create a PR?
* Any input on issue [#1141](https://github.com/w3c/data-shapes/issues/1141) (Widget scoring `hasDatatype…Constraint` shapes do not detect list-valued `sh:datatype`) before I create a PR?

## Minutes

Bergi: Keep comments concise for brevity.

Ieben: Keep discussions on topic.

## PR merge status

Bergi: Proposed a SHACL Renderer Manager: https://github.com/w3c/data-shapes/issues/1177

Ieben: PR https://github.com/w3c/data-shapes/pull/1137 contains a small editorial change for RDF 1.2 language string compatibility.

**Decision:** Merge PR https://github.com/w3c/data-shapes/pull/1137

Ieben: PR https://github.com/w3c/data-shapes/pull/1118 is still waiting on wording and phrasing changes.

Bergi: The algorithm still needs to be separated from the other responsibilities. An alternative proposal is available in PR https://github.com/w3c/data-shapes/pull/1133

Ieben: With the alternative PR, I am having trouble understanding the current objections to PR 1118. The alternative also introduces other decisions and concepts.

Robert: There are a few changes I would like to see in PR 1118. I understand the approach after hearing the explanation, but I wasn't able to understand it from the specification text alone. For example, I found the word "invoke" confusing. It would be useful for someone else to read it and see whether it makes sense to them. I don't want to block the PR over this.

Edmond: I haven't read it yet, so I can be that test.

Daniel: The alternative PR is unclear and contains other unrelated changes. I like the first part of the proposal, but I would also like to understand how it addresses the original PR.

Bergi: The relevant commit is https://github.com/w3c/data-shapes/pull/1133/changes/f5ae84c2c94d0d2d87c32af977c5a15952662dca

### SHACL Renderer Manager

Bergi: The SHACL Renderer is more focused on the rendering algorithm, while the SHACL Renderer Manager manages the surrounding concerns, such as switching between focus nodes.

Ieben: Thanks for the background context. I'd like to come back to this now that I understand the motivation.

## Global configuration

Robert: We need to discuss whether we need two label preference predicates or only one. The other question concerns the read-only graph, which was previously the inference graph. Do we still need this configuration property?

Daniel: I'm in favour of having one predicate, although I would also be fine with keeping both. Restricting the value to an IRI seems limiting. I propose optionally allowing a property path for the label preference, for example to support SKOS-XL.

Robert: That sounds like it would add some complexity, but I would be fine with it. I don't have strong feelings either way.

Bergi: Property paths are already supported elsewhere in SHACL, so this should not be too difficult for implementers.

Ieben: I am also fine with using one predicate for labels.

**Decision:** The group accepts property paths for label preferences.

Robert: Regarding data governance, I wonder whether this could complicate data governance-related tasks. Do we need the read-only graph?

Ieben: It would be good to consider the read-only graph.

Edmond: Could we also consider my proposed text for timezones?

**Action:** Task Force to review external PR https://github.com/w3c/data-shapes/pull/1112

Robert: PR 1112 may need to mention security concerns.

**Action:** Research and document security concerns relating to remote requests.
