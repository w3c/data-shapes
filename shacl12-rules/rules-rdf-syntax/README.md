==== SRL RDF Syntax

* Targets : Rule set target rules-targets.ttl
   `shacl-validate rules-targets.ttl DATA`
* Shapes : `rdf-syntax-shapes.ttl`
* RDFS: `rdf-syntax-vocab.ttl`
* Tests of shapes : `tests/`

== Run tests.

Run in the test directory.

Each test file is both shapes and data.
